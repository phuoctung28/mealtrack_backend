name: Release Pipeline

on:
  push:
    branches: [main]

permissions: {}

env:
  TEST_IMAGE: mealtrack-backend:test-${{ github.sha }}
  PROD_IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}

jobs:
  build-test-image:
    name: 🔨 Build Test Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Build test image
        run: docker build -f Dockerfile.test -t ${{ env.TEST_IMAGE }} .

      - name: Save image as artifact
        run: docker save ${{ env.TEST_IMAGE }} -o test-image.tar

      - uses: actions/upload-artifact@v4
        with:
          name: test-image
          path: test-image.tar
          retention-days: 1

  unit-tests:
    name: 🧪 Unit Tests
    needs: build-test-image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: test-image

      - name: Load test image
        run: docker load -i test-image.tar

      - name: Run unit tests
        run: |
          docker run --rm ${{ env.TEST_IMAGE }} \
            pytest tests/unit -v --cov=src --cov-fail-under=65

  trigger-e2e:
    name: 🚀 Trigger E2E Tests
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: Trigger mealtrack-e2e workflow
        uses: peter-evans/repository-dispatch@ff4da817e31db7f9deb889f7ef7a042d694ff76a
        with:
          token: ${{ secrets.E2E_REPO_PAT }}
          repository: phuoctung28/mealtrack-e2e
          event-type: run-e2e
          client-payload: '{"ref": "${{ github.sha }}", "triggered_by": "backend-release"}'

  approval:
    name: ✅ Manual Approval
    # wait-for-e2e polling was removed because it polled ref: main which does not reflect
    # this run's E2E results. The approver must manually verify E2E passed in the
    # mealtrack-e2e repository before approving this gate.
    needs: trigger-e2e
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "Release approved"

  build-push-image:
    name: 📦 Build & Push Production Image
    needs: approval
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        run: |
          docker build -t ${{ env.PROD_IMAGE }} .
          docker tag ${{ env.PROD_IMAGE }} ghcr.io/${{ github.repository }}:latest
          docker push ${{ env.PROD_IMAGE }}
          docker push ghcr.io/${{ github.repository }}:latest
